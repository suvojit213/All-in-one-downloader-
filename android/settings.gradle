pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    // We will let flutter.gradle apply the plugin
}

include ':app'

// This is the magic line for Flutter
apply from: new File(new File(flutterRoot).parentFile, 'packages/flutter_tools/gradle/plugins.gradle')